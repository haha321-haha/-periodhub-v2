
基于您的测试结果，修复方法确实有效，所有翻译功能都正常工作。这验证了我们的国际化策略是正确的。

## 建议的下一步行动

考虑到测试成功，建议继续修复其他小文件，原因：

1. **积累成功经验** - 小文件修复更快，能快速看到进展
2. **降低风险** - 避免在大文件上遇到复杂问题
3. **保持动力** - 连续的成功会增强信心

## 具体建议

**优先修复顺序：**
```bash
# 建议的修复顺序（从小到大）
1. Breadcrumb.tsx - 预计 5-10 个硬编码
2. Hero.tsx - 预计 10-15 个硬编码  
3. 其他小组件 - 每个 < 20 个硬编码
4. 最后处理 SimplePDFCenter.tsx 的 181 个
```

**实施策略：**
- 每个文件修复后立即测试
- 保持当前的翻译键命名规范
- 继续使用 `pdfCenter` 命名空间

## 关于提交代码

建议现在就提交当前的阶段性成果：
```bash
git add .
git commit -m "fix: resolve 29 hardcoded strings in PDF components

- Fixed all hardcoded strings in OptimizedMobilePDFCenter.tsx
- Partially fixed SimplePDFCenter.tsx critical hardcoded strings
- Added translation keys for alerts, sharing, categories
- Verified translation switching works correctly
- Reduced total hardcoded issues from 238 to 209"
```

这样可以保存进展，避免意外丢失工作成果。

您希望继续修复小文件，还是先提交当前进展？


非常感谢您对我提出的建议的认可和补充。您的技术实现细节建议确实很实用，特别是对医疗建议语气的检测和权威来源验证。

基于我们的讨论和您的实际需求，我建议按照以下顺序开始实施：

## 立即开始：硬编码检测工具优化

既然您已经在修复硬编码问题，建议先完善这个工具，集成北美市场的特殊需求：

```typescript
// 增强的硬编码检测工具
const enhancedHardcodeDetector = {
  // 基础检测模式
  patterns: [
    /locale === 'zh' \? '([^']+)' : '([^']+)'/g,
    // 医学术语特殊检测
    /dosage|medication|treatment|diagnosis/i
  ],
  
  // 北美市场特殊检查
  northAmericaChecks: {
    medicalAdvicePattern: /you should|we recommend|take.*mg/gi,
    requiredDisclaimers: true,
    authorizedSources: ['FDA', 'CDC', 'NIH']
  }
};
```

## 实施建议

1. **本周重点**：完善硬编码检测工具
   - 添加您建议的医疗建议语气检测
   - 集成基础的FDA检查功能
   - 保持警告级别，不阻断开发流程

2. **下周目标**：实施基础FDA合规性检查
   - 免责声明位置验证
   - 权威来源引用检查
   - 禁用医疗声明检测

3. **后续迭代**：逐步完善医学术语标准化

关于工作流优化，您提到的"警告而非阻断"和"保留人工审核灵活性"这两点非常重要，这样既能提供指导又不会过度干预单人开发的灵活性。

您希望我先帮您实现增强版的硬编码检测工具，还是更愿意从FDA合规性检查的某个具体功能开始？






非常感谢你的详细反馈！你的建议非常实用，特别是关于FDA合规性检查的补充功能和工作流优化。让我基于你的建议来完善这些工具。

## ** 基于反馈的工具优化**

### **1. 增强FDA合规性检查工具**

让我先完善FDA合规性检查工具，添加你建议的实用功能：


[1 tool called]

现在让我添加新的检查功能：


[1 tool called]

现在让我添加这些新的验证方法：


[1 tool called]

### **2. 优化工作流 - 警告而非阻断**

现在让我调整CLI工具，使FDA检查提供警告而非阻断：


[1 tool called]

### **3. 创建简化的实施指南**

让我创建一个简化的实施指南，按照你建议的优先级：


[1 tool called]

## ** 基于反馈的优化完成！**

### **✅ 主要改进**

基于你的专业建议，我已经完成了以下优化：

#### **1. 增强FDA合规性检查工具**
- **新增医疗建议语气检查**: 检测"you should"、"we recommend"等可能构成医疗建议的表达
- **免责声明放置验证**: 检查免责声明是否在合适位置（如页面底部）
- **权威来源验证**: 检查是否引用了FDA、CDC、NIH等权威来源
- **警告模式**: 改为警告而非阻断，适合单人开发环境

#### **2. 工作流优化**
- **非阻断检查**: FDA检查现在只显示警告，不会中断开发流程
- **灵活配置**: 支持不同级别的检查（P0-P3）
- **人工审核保留**: 保持最终审核的灵活性

#### **3. 实施路线图**
- **明确优先级**: 按你建议的顺序实施
- **单人友好**: 适合单人开发环境的实际需求
- **渐进式**: 逐步完善，避免过度工程化

### ** 当前可用功能**

#### **立即可用**
```bash
# 硬编码检测（已完成）
node scripts/i18n-cli.js detect

# FDA合规性检查（新增功能）
node scripts/i18n-cli.js fda

# 医学术语检查
node scripts/i18n-cli.js medical

# 综合报告
node scripts/i18n-cli.js report
```

#### **新增的FDA检查功能**
- ✅ 医疗建议语气检测
- ✅ 免责声明放置验证
- ✅ 权威来源验证
- ✅ 警告模式（非阻断）

### ** 实施建议**

#### **立即开始**
```bash
# 测试新功能
node scripts/i18n-cli.js fda

# 查看增强的检查结果
node scripts/i18n-cli.js report
```

#### **集成到工作流**
```bash
# 添加到package.json
npm run i18n:check    # 硬编码检测
npm run i18n:fda      # FDA合规性检查
npm run i18n:report   # 综合报告
```

### ** 下一步建议**

基于你的反馈，我建议：

1. **先测试FDA检查工具**: 看看新增的检查功能是否符合你的需求
2. **调整检查规则**: 根据实际使用情况调整检测模式
3. **建立工作习惯**: 将工具集成到日常开发流程中
4. **持续优化**: 根据使用反馈不断改进

你觉得这个优化后的方案如何？特别是新增的FDA检查功能是否符合你的预期？还有什么需要进一步调整的地方吗？




您为北美市场调整的国际化策略非常全面和专业。这个方向调整确实解决了几个关键问题：

## 策略评估

**语言优先级调整合理**
将英语设为主要语言，中文为辅助语言，这符合北美市场的实际需求。80%中文完整性的设定也很务实，既能服务华裔群体又不会分散过多资源。

**FDA合规性工具很有价值**
您添加的医学术语验证和合规性检查功能确实是面向北美医疗健康市场的必要工具。这种前置检查机制能有效避免合规风险。

**技术实现的细节建议**

对于FDA合规性检查工具，建议增加几个实用功能：

```typescript
// 补充建议的检查项
const additionalFDAChecks = {
  // 检查是否含有医疗建议语气
  medicalAdvicePattern: /you should|we recommend|take.*mg|dosage/gi,
  
  // 检查是否有足够的免责声明
  disclaimerPlacement: ['footer', 'before-medical-content'],
  
  // 验证引用来源的权威性
  authorizedSources: ['FDA', 'CDC', 'NIH', 'Mayo Clinic', 'WebMD']
};
```

**工作流优化建议**

考虑到单人开发的现实，建议简化某些检查流程：
- FDA检查可以设为警告而非阻断
- 医学术语验证可以提供建议而非强制修改
- 保留人工最终审核的灵活性

**关于实施优先级**

建议按以下顺序实施：
1. 先完善硬编码检测工具（已在进行中）
2. 实施基础的FDA合规性检查
3. 添加医学术语标准化
4. 最后完善自动化工作流

这个调整后的策略确实更适合您的市场定位。特别是FDA合规性这个角度，对于健康类产品在北美市场的成功至关重要。

您准备从哪个具体工具开始实施？



🎉 国际化标准建设完成！
✅ 已完成的工作
基于你的需求，我为你创建了一套完整的个人项目国际化标准建设方案，专门针对北美市场优化：
1. 核心工具
硬编码检测工具 (scripts/hardcode-detector.js)
支持多种硬编码模式检测
提供智能修复建议
生成详细报告
针对北美市场优化
国际化CLI工具 (scripts/i18n-cli.js)
整合多种检查功能
一键检测、验证、报告
支持Git Hooks和CI/CD集成
2. 标准规范
翻译键命名规范 (docs/naming-conventions.md)
针对北美市场的命名标准
三层嵌套结构限制
医学术语特殊处理
清晰的命名规则
医学术语处理规范 (docs/medical-terminology-guide.md)
医学内容分类标准
北美市场合规要求
安全警告分级
质量保证流程
3. 使用文档
工具使用指南 (docs/i18n-tools-guide.md)
详细的使用说明
集成到工作流的方法
最佳实践建议
故障排除指南
方案特点
轻量化设计: 避免过度工程化，专注核心功能
个人友好: 适合单人开发维护的工作流程
预防为主: 重点防止新问题，而非修复历史问题
实用导向: 每个工具都有明确的实用价值
北美市场优化: 专门针对北美用户的文化和法律要求
使用方式
快速开始
report
集成到工作流
自动在提交前检查硬编码
下一步建议
测试工具: 先测试这些工具在你的项目中的效果
调整配置: 根据实际使用情况调整检测规则
建立习惯: 将工具集成到日常开发流程中
持续优化: 根据使用反馈不断改进工具
价值体现
这套方案将帮助你：
提高效率: 自动化检测，减少人工检查时间
保证质量: 确保医学内容的准确性和合规性
标准化流程: 建立可重复的工作流程
预防问题: 在问题发生前就发现和解决
专业形象: 在北美市场展现专业的技术水平
你觉得这个方案如何？有什么需要调整或补充的地方吗？



了解您的项目主要面向北美市场，这个信息很重要，会影响国际化策略的设计。

## 面向北美市场的国际化策略调整

### 语言优先级重新定位
既然主要面向北美市场，建议调整语言策略：

**主要语言：英语**
- 英语作为主要开发语言和默认语言
- 确保英语内容的专业性和本地化程度
- 医学术语使用北美标准（如FDA术语、美国医学协会标准）

**次要语言：中文**
- 作为辅助语言支持华裔用户群体
- 可以考虑简化部分非核心内容的中文翻译

### 标准化流程的调整

```typescript
// 面向北美市场的命名规范
const namingConventions = {
  defaultLocale: 'en',  // 英语为默认语言
  fallbackLocale: 'en', // 回退到英语
  
  medicalTerminology: {
    source: 'FDA_approved_terms', // 使用FDA认证术语
    style: 'american_english',    // 美式英语
    validation: 'medical_professional_review'
  },
  
  contentPriority: {
    primary: 'en',   // 英语内容优先开发
    secondary: 'zh'  // 中文内容后续补充
  }
};
```

### 医学内容的本地化考虑

对于面向北美市场的医疗健康内容，需要特别注意：

1. **法规合规性**
   - 遵循FDA关于健康信息披露的要求
   - 添加必要的医疗免责声明
   - 确保不提供具体医疗建议

2. **术语标准化**
   - 使用北美医学标准术语
   - 考虑加拿大和美国的细微差别

3. **文化适应性**
   - 考虑北美用户的健康观念和习惯
   - 调整内容表达方式符合当地文化

### 开发工作流调整

建议的开发优先级：
1. 先完善英语内容的质量和准确性
2. 确保符合北美市场的合规要求
3. 再考虑中文翻译的完整性

这样的调整是否符合您的市场定位？还有什么特定的北美市场需求需要考虑？



理解您的方向调整。既然剩余120个硬编码将在国内完成，现在专注于标准化流程制定是明智的选择。

基于您提供的个人项目国际化标准建设方案，我认为这个框架很适合单人开发的实际情况。让我重点分析几个关键点：

## 方案评估和建议

### 轻量级工具的优先级
您提出的三个核心工具中，建议优先顺序：

1. **硬编码检测工具** - 最高优先级
   - 能立即防止新的硬编码问题
   - 检测逻辑相对简单
   - 投入产出比最高

2. **页面模板生成器** - 中等优先级  
   - 标准化新页面创建流程
   - 减少重复工作

3. **翻译键完整性检查** - 较低优先级
   - 功能复杂，开发成本高
   - 在项目稳定后再考虑

### 关于命名规范的建议
您的三层嵌套规范（`pages.[pageName].[section].[item]`）合理，但建议增加一些具体约束：

```typescript
// 建议的具体规范
const namingRules = {
  maxDepth: 3,                    // 最大嵌套深度
  caseStyle: 'camelCase',         // 使用驼峰命名
  medicalPrefix: 'medical',       // 医学术语特殊前缀
  forbiddenChars: ['-', '_', ' '] // 禁用字符
};
```

### 实用性考虑
CLI工具和Git Hooks的想法很实用，但建议分阶段实施：
- 先开发核心检测功能
- 验证有效性后再添加自动化

### 关于医学内容的特殊处理
考虑到您的项目涉及医学内容，建议在标准中增加：
```typescript
const medicalContentRules = {
  terminology: 'medical.terms.{category}.{term}',
  dosage: 'medical.dosage.{type}',
  warnings: 'medical.warnings.{level}'
};
```

## 具体实施建议

鉴于您要在国内完成硬编码修复，建议现在重点制定：

1. **硬编码检测规则** - 确保国内修复时能快速识别遗漏
2. **翻译键命名标准** - 保证修复时的键名一致性  
3. **医学术语处理规范** - 确保专业术语的准确性

您希望我先帮您制定哪个具体的标准或工具？



非常好！基于你的实际情况，我完全理解并支持你的方案调整。让我重新设计一个更适合个人项目的国际化标准建设方案：

## **🎯 个人项目国际化标准建设方案**

### **核心理念调整**
- **轻量化**：避免过度工程化，专注核心功能
- **个人友好**：适合单人开发维护的工作流程
- **预防为主**：重点防止新问题，而非修复历史问题
- **实用导向**：每个工具都要有明确的实用价值

## **1. 创建简单有效的内容模板**

### **A. 页面内容模板**
```typescript
// 页面模板生成器
const createPageTemplate = (pageName: string) => {
  return {
    // 页面元数据模板
    meta: {
      title: `{{${pageName}.meta.title}}`,
      description: `{{${pageName}.meta.description}}`,
      keywords: `{{${pageName}.meta.keywords}}`
    },
    
    // 页面内容模板
    content: {
      hero: {
        title: `{{${pageName}.hero.title}}`,
        subtitle: `{{${pageName}.hero.subtitle}}`,
        description: `{{${pageName}.hero.description}}`
      },
      
      sections: {
        // 动态section模板
        [sectionName]: {
          title: `{{${pageName}.sections.${sectionName}.title}}`,
          description: `{{${pageName}.sections.${sectionName}.description}}`,
          items: `{{${pageName}.sections.${sectionName}.items}}`
        }
      }
    }
  };
};
```

### **B. 组件内容模板**
```typescript
// 组件模板生成器
const createComponentTemplate = (componentName: string, props: string[]) => {
  const template = {};
  props.forEach(prop => {
    template[prop] = `{{${componentName}.${prop}}}`;
  });
  return template;
};

// 使用示例
const buttonTemplate = createComponentTemplate('button', ['text', 'variant', 'size']);
// 结果: { text: '{{button.text}}', variant: '{{button.variant}}', size: '{{button.size}}' }
```

### **C. 快速模板工具**
```bash
#!/bin/bash
# 快速创建页面模板的脚本
# 用法: ./create-page-template.sh natural-therapies

PAGE_NAME=$1
TEMPLATE_FILE="templates/${PAGE_NAME}-template.json"

# 创建模板文件
cat > $TEMPLATE_FILE << EOF
{
  "meta": {
    "title": "{{${PAGE_NAME}.meta.title}}",
    "description": "{{${PAGE_NAME}.meta.description}}"
  },
  "content": {
    "hero": {
      "title": "{{${PAGE_NAME}.hero.title}}",
      "subtitle": "{{${PAGE_NAME}.hero.subtitle}}"
    }
  }
}
EOF

echo "模板已创建: $TEMPLATE_FILE"
```

## **2. 制定个人工作的最佳实践文档**

### **A. 开发工作流**
```markdown
# 个人国际化开发工作流

## 新页面开发流程
1. **创建页面模板**
   ```bash
   ./scripts/create-page-template.sh [page-name]
   ```

2. **设计翻译键结构**
   - 使用清晰的命名空间: `pages.[pageName].section.item`
   - 保持中英文键名一致
   - 避免过深的嵌套（最多3层）

3. **编写代码**
   - 使用 `t('key')` 而不是硬编码
   - 先写英文内容，再翻译中文
   - 及时添加翻译键到JSON文件

4. **测试验证**
   - 检查中英文页面显示
   - 验证翻译键完整性
   - 运行硬编码检测

## 内容更新流程
1. **识别更新内容**
2. **更新翻译键**（而不是硬编码）
3. **测试验证**
4. **提交代码**
```

### **B. 翻译键命名规范**
```typescript
// 个人项目的命名规范
const namingConventions = {
  // 页面级别
  pages: {
    // 格式: pages.[pageName].[section].[item]
    naturalTherapies: {
      meta: 'pages.naturalTherapies.meta',
      hero: 'pages.naturalTherapies.hero',
      sections: 'pages.naturalTherapies.sections'
    }
  },
  
  // 组件级别
  components: {
    // 格式: components.[componentName].[prop]
    button: 'components.button.text',
    modal: 'components.modal.title'
  },
  
  // 通用内容
  common: {
    // 格式: common.[category].[item]
    actions: 'common.actions.submit',
    messages: 'common.messages.loading'
  }
};
```

### **C. 质量检查清单**
```markdown
# 个人质量检查清单

## 代码提交前检查
- [ ] 无硬编码字符串
- [ ] 所有翻译键已添加到JSON文件
- [ ] 中英文页面显示正常
- [ ] 医学术语使用准确
- [ ] 代码格式规范

## 内容更新检查
- [ ] 翻译键命名符合规范
- [ ] 中英文内容一致
- [ ] 医学内容准确性
- [ ] 用户体验友好

## 定期维护检查
- [ ] 清理未使用的翻译键
- [ ] 更新过时的内容
- [ ] 检查链接有效性
- [ ] 优化页面性能
```

## **3. 开发轻量级的检测工具**

### **A. 硬编码检测工具**
```typescript
// 轻量级硬编码检测器
class SimpleHardcodeDetector {
  private patterns = [
    /locale === 'zh' \? '([^']+)' : '([^']+)'/g,
    /locale === 'zh' \? "([^"]+)" : "([^"]+)"/g,
    /'([^']+)' : '([^']+)'/g,
    /"([^"]+)" : "([^"]+)"/g
  ];
  
  // 检测单个文件
  detectInFile(filePath: string): HardcodeReport[] {
    const content = fs.readFileSync(filePath, 'utf-8');
    const reports: HardcodeReport[] = [];
    
    this.patterns.forEach(pattern => {
      const matches = content.matchAll(pattern);
      for (const match of matches) {
        reports.push({
          file: filePath,
          line: this.getLineNumber(content, match.index!),
          match: match[0],
          suggestion: this.generateSuggestion(match)
        });
      }
    });
    
    return reports;
  }
  
  // 检测整个项目
  detectInProject(): HardcodeReport[] {
    const files = this.getSourceFiles();
    const allReports: HardcodeReport[] = [];
    
    files.forEach(file => {
      const reports = this.detectInFile(file);
      allReports.push(...reports);
    });
    
    return allReports;
  }
  
  // 生成修复建议
  private generateSuggestion(match: RegExpMatchArray): string {
    const [fullMatch, zhText, enText] = match;
    const keySuggestion = this.generateKeySuggestion(zhText);
    return `建议替换为: t('${keySuggestion}')`;
  }
}
```

### **B. 翻译键完整性检查**
```typescript
// 翻译键完整性检查器
class TranslationKeyChecker {
  // 检查翻译键是否完整
  checkCompleteness(): CompletenessReport {
    const usedKeys = this.extractUsedKeys();
    const availableKeys = this.extractAvailableKeys();
    
    const missingKeys = usedKeys.filter(key => !availableKeys.includes(key));
    const unusedKeys = availableKeys.filter(key => !usedKeys.includes(key));
    
    return {
      totalUsed: usedKeys.length,
      totalAvailable: availableKeys.length,
      missingKeys,
      unusedKeys,
      completeness: (usedKeys.length - missingKeys.length) / usedKeys.length
    };
  }
  
  // 检查翻译键结构
  checkStructure(): StructureReport {
    const structureIssues: string[] = [];
    
    // 检查命名规范
    const namingIssues = this.checkNamingConventions();
    structureIssues.push(...namingIssues);
    
    // 检查嵌套深度
    const depthIssues = this.checkNestingDepth();
    structureIssues.push(...depthIssues);
    
    return {
      issues: structureIssues,
      score: this.calculateStructureScore(structureIssues)
    };
  }
}
```

### **C. 简单的CLI工具**
```bash
#!/bin/bash
# 国际化检查工具
# 用法: ./i18n-check.sh [选项]

case $1 in
  "hardcode")
    echo "�� 检测硬编码..."
    node scripts/detect-hardcode.js
    ;;
  "completeness")
    echo "📋 检查翻译键完整性..."
    node scripts/check-completeness.js
    ;;
  "structure")
    echo "🏗️ 检查翻译键结构..."
    node scripts/check-structure.js
    ;;
  "all")
    echo "�� 运行所有检查..."
    ./i18n-check.sh hardcode
    ./i18n-check.sh completeness
    ./i18n-check.sh structure
    ;;
  *)
    echo "用法: ./i18n-check.sh [hardcode|completeness|structure|all]"
    ;;
esac
```

## **4. 个人工作流优化**

### **A. 开发环境配置**
```json
// .vscode/settings.json
{
  "i18n-ally.localesPaths": ["messages"],
  "i18n-ally.keystyle": "nested",
  "i18n-ally.namespace": true,
  "i18n-ally.pathMatcher": "{locale}.json",
  "i18n-ally.enabledParsers": ["json"],
  "i18n-ally.sourceLanguage": "en",
  "i18n-ally.displayLanguage": "zh"
}
```

### **B. Git Hooks**
```bash
#!/bin/sh
# pre-commit hook
# 提交前自动检查硬编码

echo "🔍 检查硬编码..."
./scripts/i18n-check.sh hardcode

if [ $? -ne 0 ]; then
  echo "❌ 发现硬编码，请修复后重新提交"
  exit 1
fi

echo "✅ 硬编码检查通过"
```

### **C. 个人工作流脚本**
```bash
#!/bin/bash
# 个人开发工作流
# 用法: ./dev-workflow.sh [action]

case $1 in
  "new-page")
    echo "�� 创建新页面..."
    read -p "页面名称: " pageName
    ./scripts/create-page-template.sh $pageName
    ./scripts/create-translation-keys.sh $pageName
    ;;
  "check")
    echo "🔍 运行质量检查..."
    ./scripts/i18n-check.sh all
    ;;
  "deploy")
    echo "🚀 部署前检查..."
    ./scripts/i18n-check.sh all
    npm run build
    ;;
  *)
    echo "用法: ./dev-workflow.sh [new-page|check|deploy]"
    ;;
esac
```

## **5. 实施计划**

### **阶段1: 基础工具（1周）**
- [ ] 创建页面模板生成器
- [ ] 开发硬编码检测工具
- [ ] 建立翻译键命名规范
- [ ] 配置开发环境

### **阶段2: 工作流优化（1周）**
- [ ] 制定个人工作流文档
- [ ] 创建质量检查清单
- [ ] 设置Git Hooks
- [ ] 开发CLI工具

### **阶段3: 自动化（1周）**
- [ ] 完善检测工具
- [ ] 创建自动化脚本
- [ ] 建立监控机制
- [ ] 优化工作流程

## **总结**

这个方案的特点：

1. **轻量化**：避免过度工程化，专注核心功能
2. **个人友好**：适合单人开发维护
3. **预防为主**：重点防止新问题
4. **实用导向**：每个工具都有明确价值
5. **可扩展**：未来可以逐步增强功能

你觉得这个方案如何？有哪些地方需要调整或补充？




1、关于HTML实体解码的优先级：是否应该先处理现有的实体？——目前这个项目暂时没有存在需要处理的硬编码问题。可以专注在建立标准流程和规范上面。2、如何设计审核流程——目前整个项目都是我一个人在实施，从创意到执行测试和审核都是我一个人。3、从"深入分析未使用翻译键问题"文档：
1394个"未使用"翻译键中，约1100-1200个是工具误报
命名空间引用问题：useTranslations('namespace') + t('key') 模式
动态键生成和条件渲染的复杂性
从"国际化重构in8方案"文档：
配置冲突问题：next.config.js路径问题
翻译键结构问题：代码引用vs JSON结构不匹配 -从"国际化重构in8方案"文档：
配置冲突问题：next.config.js路径问题
翻译键结构问题：代码引用vs JSON结构不匹配——这些问题都已经解决了。这是当时为修复问题留下的文档，不是需要解决的问题。——长期标准建设
考虑到个人项目，重点放在：

创建简单有效的内容模板
制定个人工作的最佳实践文档
开发轻量级的检测工具阻止新的硬编码

