'use client'; import {useTranslations, useLocale} from 'next-intl'; export const useAppTranslations = (namespace?: string) => {const t = useTranslations(namespace); const locale = useLocale(); return {t, locale, isZh: locale === 'zh', isEn: locale === 'en'}}; export const useInteractiveToolTranslations = (toolName?: string) => {const namespace = toolName ? `interactiveTools` : 'interactiveTools'; const t = useTranslations(namespace); const locale = useLocale(); return {t: toolName ? (key: string) => t(`${toolName}.${key}`) : t, locale, isZh: locale === 'zh', isEn: locale === 'en'}}; export const useTranslatedOptions = (namespace: string, optionsKey: string) => {const {t} = useAppTranslations(namespace); return (options: string[]) => {return options.map(option => ({value: option, label: t(`${optionsKey}.${option}`)}))}};